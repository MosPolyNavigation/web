export enum CardState {
  HIDDEN = 'HIDDEN',
  COLLAPSED = 'COLLAPSED',
  EXPANDED = 'EXPANDED',
}

export enum Pos {
  BOTTOM = 'BOTTOM',
  MIDDLE = 'MIDDLE',
  LEFT = 'LEFT',
  RIGHT = 'RIGHT',
}

export enum Color {
  INITIAL = 'none',
  C1 = '#131416',
  C2 = '#1C1F26',
  ACTIVE = '#1D253A',
  C3 = '#4C515C',
  C4 = '#A0A4AD',
  C5 = '#D9D9D9',
  BLUE = '#9CBBFF',
  CYAN = '#63E3D4',
  VIOLET = '#A091F8',
}

export enum ActionName {
  SHOW = 'SHOW',
  HIDE = 'HIDE',
}

export enum Size {
  XS,
  S,
  SM,
  M,
  L,
}

export enum BtnName {
  MENU = 'MENU',
  MENU_CLOSE = 'MENU_CLOSE',
  SEARCH = 'SEARCH',
  FAVOURITE = 'FAVOURITE',
  BOTTOM_RIGHT = 'BOTTOM_RIGHT',
  BOTTOM_CARD_CLOSE = 'BOTTOM_CARD_CLOSE-close',
}

export enum Layout {
  PLAN = 'PLAN',
  LOCATIONS = 'LOCATIONS',
  SEARCH = 'SEARCH',
  MENU = 'MENU',
}

export enum SearchIndent {
  SELECT = 'SELECT',
  SET_FROM = 'SET_FROM',
  SET_TO = 'SET_TO',
}
